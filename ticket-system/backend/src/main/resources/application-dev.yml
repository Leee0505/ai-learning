spring:
  kafka:
    bootstrap-servers: 192.168.50.208:9092
  datasource:
    url: jdbc:mysql://192.168.50.208:3306/ticket_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: jw-0505
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 192.168.50.208
      port: 6379
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

jwt:
  secret: ${JWT_SECRET:dev-secret-key-that-is-at-least-256-bits-long-for-hs256}

# Local disk storage — matches storage.type=local (default)
storage:
  type: local
  local:
    upload-dir: ./uploads
